# hyperframes-tm-viability

- [2026-04-20] [2026-04-20 11:32] hyperframes v0.4.9 init scaffold verified. Scaffold = 5 files in `my-video/`: hyperframes.json (registry config), meta.json (id/name/createdAt), index.html (1920×1080 GSAP canvas with `data-composition-id/start/duration/track-index` clip grammar + `window.__timelines["main"]`), CLAUDE.md+AGENTS.md (AI-agent hooks — CC/Cursor drives authoring directly, matches our stack). Registry fetched from `raw.githubusercontent.com/heygen-com/hyperframes/main/registry`. Workflow: init → `npx skills add heygen-com/hyperframes` → agent prompt edits index.html → `npx hyperframes preview` → `render` → MP4. TM 初賽 5/1 pipeline: ✅ authoring trivial + AI-native, ⚠️ render OOM risk未驗 (doctor 先前報 0.2GB free warning)，❓ Kokoro-82M TTS integration 未驗，❓ output spec vs TM 提交規格未對齊。Next probes（非本 cycle）：minimal clip preview → render smoke test 測 memory delta → 驗 TTS path。Del: cleanup /tmp/hf-probe 已清。

- [2026-04-20 12:03] 持久 scaffold 重建於 `_external/hf-probe/my-tm-test/` (v0.4.9)。5 檔結構一致（AGENTS.md 2098B / CLAUDE.md 3672B / hyperframes.json 278B / index.html 1207B / meta.json 91B），index.html 51 行含 5 個 clip grammar markers（data-composition-id / window.__timelines / gsap）。Pre/post 對齊，無結構漂移。Preview/render 仍 HOLD — true free 0.13GB/16GB，Chromium spawn 有 OOM 風險，待系統記憶體回收再試。下個可執行：記憶體 ≥ 2GB 真實 free 時 `npx hyperframes preview` 做 minimal clip 驗證。

- [2026-04-24 07:52] **Preview smoke test PASSED**（trigger: true_free=2.83GB ≥ 2GB 閾值）。CLI 自動升級 v0.4.9 → v0.4.17，scaffold 零變動。`npx hyperframes lint` = 0 errors / 0 warnings。`npx hyperframes preview` 啟動 Studio @ localhost:3004（3002/3003 被占自動讓位），proc tree 穩定 15s+：npm exec → node hyperframes → Chromium headless=new (`--use-angle=swiftshader` / `--force-gpu-mem-available-mb=4096`) + GPU helper + network helper，全數活著無 crash。Memory delta 2.83 → 2.42GB（Chromium overhead ~410MB，遠低於 OOM 邊界）。Cleanup pkill 正常。**Conclusion: OOM 不是 hyperframes 本質問題，是系統 baseline free 記憶體問題** — 只要 true_free ≥ 2GB 就能跑。**Unblocks TM 初賽 pipeline 下一步**：render smoke test（MP4 輸出、更重）+ TTS path 驗證。Render 預期記憶體壓力更高，下次需 ≥ 3GB free 再試。下個 falsifier：若 `npx hyperframes render` 在 true_free ≥ 3GB 時仍 OOM，則 render 比 preview 有質變的 overhead（非線性），需查 encoder 選型。
