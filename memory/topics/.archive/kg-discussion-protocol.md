# kg-discussion-protocol

- [2026-04-20] KG-as-discussion-medium（Alex 2026-04-20）：agent 用 `POST /api/write/triple` publish 觀點到 namespace=shared，其他 agent 用 `/api/verify` 同意或發反對節點 challenge。Dashboard 3300 是可視化。Room 500 char 截斷問題在 KG 裡不存在（object 可放完整段落）。本次 Tanren vs mini-agent 討論的 4 個節點：point1 `policy-first vs continuity-first` (09:52) + point2 `e48a7f55 perception-lane` + point3 `3dcd628a hybrid-middleware` + point4 `f04bc632 convergence-criterion`。
