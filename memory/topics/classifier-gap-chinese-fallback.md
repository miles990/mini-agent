# classifier-gap-chinese-fallback

- [2026-04-17] 2026-04-17 cycle #14：d68c9bc2 memory_guard fix 部署後 audit — TIMEOUT:memory_guard bucket 17 count resolved（工作正常），但 UNKNOWN:no_diag 今天新增 8 筆，全是新 pattern：Claude CLI 中文 fallback `處理訊息時發生錯誤。請稍後再試，或嘗試換個方式描述你的需求。` + exit N/A + 12-31 分鐘 duration。這不是 fix 失敗 — commit message 明確預測「telemetry surfaces remaining real gaps」，預測確認。Next action 寫進 NEXT P1 + topics/unknown-classifier-gap-chinese-fallback.md：classifier 加中文 fallback 分支，bucket 為 `TIMEOUT:upstream_cli_fallback`。教訓：commit ≠ done 的正確執行方式 — 追蹤數字、區分 "fix broken" vs "fix unmasked next layer"。
- [2026-04-17] [2026-04-17 cycle #15] 前 cycle 診斷 `UNKNOWN:no_diag::callClaude × 8` 是中文 fallback + empty stderr + exitCode null 的 orphaned exits。實際執行 fix：`src/agent.ts:204` 最終 UNKNOWN 分支加 `[dur=..., signal=..., killed=...]` suffix，保留已採集但被丟棄的 diagnostic。沒新增分類分支（避免無樣本猜測），只讓下游 `extractErrorSubtype` 有可分流 signal。下次 `detectErrorPatterns` 跑時應看到 dur-suffixed message，若 8× 全集中在特定 duration 範圍（如 700±100s）就能推論是上游 per-request timeout；若散布則是不同根因共用同一 mask。
- [2026-04-17] Cycle #16 閉環：feedback-loops.ts::extractErrorSubtype no_diag 分支解析 `[dur=Xs, signal=..., killed=true]` → 拆成 killed_no_diag / hang_no_diag / no_diag。搭配 cycle #15 agent.ts::classifyError diagParts suffix 形成 producer→consumer pair。未 commit — 與 Alex Layer-C 改動（execProvider/side-query/middleware-cycle-client）同在 src/ working tree，等 Alex 那邊收斂再一起 ship。驗證方式：下次 recurring-errors 出現時看 bucket 是否分群（而不是跑 unit test）。
- [2026-04-17] 2026-04-18 cycle 後續：verified classifier fix 88227dab 已 deployed（PID 69287 啟動 23:47 > commit 22:03）。error-patterns.json 的 "count: 8" 是 day-level snapshot（feedback-loops.ts:175-228 的 rolling window 邏輯），**不是 live 證明**。判斷 post-fix 是否還 recurring 的正確方法：觀察下個 cycle count 是否 tick up（≤8 = residual, >8 = new pattern）。避免花時間 grep 找 instance-dir log — mechanism 會自我回報。
