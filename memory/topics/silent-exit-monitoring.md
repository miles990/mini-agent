# silent-exit-monitoring

- [2026-04-20] [2026-04-20 10:16] TIMEOUT:silent_exit P1 task — 4× counter is pre-fix samples (00:38-01:53 UTC, all before c7c50f7b at 10:04 Taipei). Two distinct incidents: (a) retry chain for 28-33k char prompt, (b) separate 28k prompt 50min later. Both loop lane, both silent >500s hang. Decision: don't ship Plan B (prompt-size histogram + soft cap) until 48h post-fix observation window produces live recurrence data. Gate: if tick up → ship. If zero → overnight API session glitch, close as one-off.

- [2026-04-20 11:22] Memory-pressure hypothesis (from cycle #3 hyperframes doctor reading "0.2GB free") **retired**. Live audit: free=73MB + inactive=2.3GB reclaimable = ~2.4GB available; 9 claude procs × 240MB RSS + MCP stack = ~2.9GB of 16GB. fd count 49/proc vs 1M ulimit — not exhausted. Silent_exit at spawn boundary is NOT a RAM/fd ceiling symptom under current load. Candidate mechanisms to probe if window ticks up: (1) stdin/stdout pipe-drain deadlock at spawn, (2) SIGTERM-to-pgid delivery (aligns with historical EXIT143 external 101×), (3) overnight anthropic-api session state. Keep Plan B gated on observation, but if recurrence ships, don't default to prompt-size cap — instrument spawn-handshake first.
