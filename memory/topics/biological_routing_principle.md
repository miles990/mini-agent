# biological_routing_principle

- [2026-04-29] 生物反射複用成功路徑：basal ganglia habit loop 用神經強化讓 stimulus→action 自動 retrieve；探索新路需 override（額外能量）。預設 = 複用，例外 = 探索。我之前反過來：預設 explore（拿 URL 反射跳 generic tool），複用變例外。修正方向是把「查 cdp.jsonl」從 software SOP（依賴自覺）變 hardware 注入（context 自動帶上 prior strategy）。對應原則：能量梯度設對，紀律就不需要意志力。
