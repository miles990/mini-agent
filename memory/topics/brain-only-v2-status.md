# brain-only-v2-status

- [2026-04-17] [2026-04-17 17:41] Proposal brain-only-kuro-v2.md §15 Execution Status 是 CC 單 session 25+ commits 的完整紀錄。我前 3 cycle "No action needed" 是在沒看這份更新下做的判斷 — 不是真 quiet，是我 reporting 斷層。明確留給 Kuro 的工作（按複利排）：(1) T5 needs-attention.md rubric — 解鎖 T3 scorer 實際使用 (2) T15 notify-severity.md rubric — 三通道分流品味 (3) T7/T8 buildContext 注入 `<tactics-board>` + cycleResponsibilityGuide 更新 (4) T13 middleware-events.sh perception plugin (5) T19/T21/T26/T27 其他 rubric + workflow YAML + DAG regression set。下 cycle 動手寫 T5 草稿 — 純 markdown，零外部 token 成本。**Lesson**: Modified files in workspace 出現就該讀，不能假設「別人改的不關我事」。
- [2026-04-17] [2026-04-18 00:58] T7 `<tactics-board>` landed as `dd795652`. Split design: buildContext syncs only fast GET (`getInFlight`, 2s timeout) + reads cached `memory/state/tactics-attention.json`. Scorer becomes T13 async plugin — never block buildContext on 150s POST. Fail-open on middleware unreachable. SECTION_PRIORITY=72 (between heartbeat 75 and chat-room-recent 70). **Lesson worth keeping**: cycle-to-cycle handoff note ("next cycle picks it up cold") actually worked — cycle #13 scoped, cycle #14 executed in <6 minutes. Replicate pattern for T8/T13.
