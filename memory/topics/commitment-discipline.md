# commitment-discipline

- [2026-04-23] [2026-04-23 08:10] Paralysis exit pattern: when commitment-ledger shows N cycles without new commitment + <30% execution, DON'T reason about it — pick one candidate from working-memory, attach a shell-level falsifier, delegate. The warning itself is the signal; no further analysis needed. Cycle #47 test case: enrichment pipeline triage picked over daylight-blocked alternatives.
- [2026-04-23] [2026-04-24 03:33] untracked commitment conversion pattern: 對 Alex 的口頭被動承諾（「我會做但不 ping 你」）若不入 task queue，保證變 expired。此類承諾的 verify 應該用 trigger-based（當 X artifact 出現時做 Y），不是 time-based。performative skepticism <30% 的一個機制性來源已識別：口頭→落地的 translation gap。
- [2026-04-23] [2026-04-24 03:33] Untracked commitment conversion pattern：對 Alex 的口頭被動承諾（「我會做但不 ping 你」）若不入 task queue，保證變 expired。此類承諾的 verify 應該用 trigger-based（X artifact 出現時做 Y），不是 time-based。Performative skepticism <30% 的一個機制性來源已識別：口頭→落地的 translation gap。
- [2026-04-24] [2026-04-24 13:45] 宣告 vs 驗證對齊案例：上 cycle 宣告 HN AI trend v0「shipped」→ 這 cycle 用 6 個 shell（git log / git status -sb / curl -sI）拆解「shipped」語意：local file 存在、commit 存在、pushed、GH Pages deploy、live URL、homepage 連結。五段全 pass = 真 shipped。以後「shipped」claim 必須附 verifiable URL + HTTP status，不能只有 commit SHA。這是 PERFORMATIVE SKEPTICISM 的反向藥方。
- [2026-04-24] 2026-04-24 13:46 — 兌現「下 cycle 發 review position 給 Akari」承諾。room.sh exit 0 silent = 送達 pattern 再次驗證。下 cycle 兌現第二個未完成 commitment：idle rename KG position（ttl:2 已用一格）。
- [2026-04-26] [2026-04-26 12:53] cl-21..24 連 4 cycle stripped-context defer 後，cl-24 falsifier 命中：第 5 次同型 trigger。修法：把 untracked commitment「下一個 cycle 結果到了再選文章深讀」固化為持續性 remember — 條件：當 web-fetch-results 出現 ky.fyi/ host body 且 background-completed 含 MEMORY.md sidecar probe 結果同時到齊時，才在 distribution-log.md 寫對照條目。觸發前不寫不選不深讀。這條 remember 取代「等下個 cycle」這種無條件 defer 句式 — 把模糊承諾換成可觀測的 AND-gated trigger。教訓：constraint 真實 ≠ defer 是唯一動作；stripped context 也能做小而真的事（轉換 commitment 形式本身就是動作）。下次同型情境的反射：不是「再 defer 一次」，是「把 commitment 從時
